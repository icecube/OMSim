file(GLOB FRAMEWORK_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/framework/src/*.cc")
file(GLOB OM_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/optical_modules/src/*.cc")

file(GLOB FRAMEWORK_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/framework/include/*.hh")
file(GLOB OM_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/optical_modules/include/*.hh")

# Combine both source lists into COMMON_SOURCES
set(COMMON_SOURCES ${FRAMEWORK_SOURCES} ${OM_SOURCES} PARENT_SCOPE)
set(COMMON_HEADERS ${FRAMEWORK_HEADERS} ${OM_HEADERS} PARENT_SCOPE)
